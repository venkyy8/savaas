version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.12
    commands:
      - yum update
      - yum install -y awscli
  build:
    commands:
      - aws s3 cp /opt/venkat.txt s3://personnel01venky01/
